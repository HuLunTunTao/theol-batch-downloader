{
  "name": "college-resource-downloader",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build/build.mjs",
    "package": "npm run build && node build/package.mjs"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
